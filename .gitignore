# Regenerable Stage-1 outputs (run: python -m src.preprocess --country <ISO3>)
data/intermediate/

# Raw EMA Workbench experiment archives (regenerable: python -m src.run_experiments ...)
# Now written into per-country subfolders (results/<ISO3>/), so match at any depth.
# Summary CSVs and figures in results/ are kept.
results/**/*.tar.gz
results/*.tar.gz

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local agent / IDE config, not project content
.claude/
.vscode/

# OS cruft
Thumbs.db
.DS_Store
